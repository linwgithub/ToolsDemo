<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linw.tudoulin">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".ui.activity.MainActivity"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <layout
                android:defaultHeight="200dp"
                android:defaultWidth="100dp"
                android:gravity="top|end"
                android:minHeight="100dp"
                android:minWidth="100dp" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.TOOTActivity" />
        <activity android:name=".ui.activity.TabActivityDemoActivity" />
        <activity android:name=".ui.fragment.Tab1Activity" />
        <activity android:name=".ui.fragment.Tab2Activity" />
        <activity android:name=".ui.activity.TabLayoutDemoActivity" />
        <activity android:name=".ui.activity.ShowHorizontalListView" />
        <activity android:name=".ui.activity.BindingTextActivity" />
        <activity android:name=".ui.activity.AdjustAndoridNActivity"></activity>
    </application>

</manifest>